// accessing properties on primitive values

// how to access .length of some string value
// it is accessed with boxing aka implicit coercion
if(studentNameElem.value.length > 50)
{
    console.log("Students name too long.");
}